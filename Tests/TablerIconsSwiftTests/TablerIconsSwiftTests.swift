import XCTest
@testable import TablerIconsSwift

final class TablerIconsSwiftTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TablerIconsSwift().text, "Hello, World!")
    }
}
